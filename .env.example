# ──────────────────────────────────────────
# Pali Docs — Environment Variables
# ──────────────────────────────────────────
# Copy this file to .env.local and fill in your values.
# Never commit .env.local to version control.

# ── Pinecone (Vector Database) ────────────
# Required for RAG chat feature.
# Sign up: https://www.pinecone.io
PINECONE_API_KEY=
PINECONE_INDEX_NAME=
# Optional: scopes queries to a namespace within the index
PINECONE_NAMESPACE=

# ── LLM Provider ───────────────────────────
# Required for AI chat, quiz generation, and follow-up suggestions.
# Set PROVIDER_NAME to "openrouter" (default) or "opencode".
PROVIDER_NAME=

# OpenRouter (default): https://openrouter.ai
OPENROUTER_API_KEY=
# Default model: google/gemma-3-27b-it:free
OPENROUTER_LLM_MODEL=

# OpenCode (OpenAI-compatible): https://opencode.ai
# Available models: deepseek-v4-flash, deepseek-v4-pro, gpt-5.4-mini, etc.
OPENCODE_API_KEY=
OPENCODE_LLM_MODEL=

# ── Algolia (Full-Text Search) ────────────
# Required for the documentation search UI and indexing script.
# Sign up: https://www.algolia.com
NEXT_PUBLIC_ALGOLIA_APP_ID=
NEXT_PUBLIC_ALGOLIA_SEARCH_API_KEY=
# Admin API key — SERVER-SIDE ONLY (used by `bun run index`).
# Must NOT have the NEXT_PUBLIC_ prefix, or it will be inlined into the
# client bundle and exposed to every visitor.
ALGOLIA_ADMIN_API_KEY=
# Optional: for Algolia Assistant / AI search features
NEXT_PUBLIC_ALGOLIA_ASSISTANT_ID=
